import 'package:flutter/material.dart';
import 'package:kksc_app_fe/util/cosnt_value.dart';

TextStyle displayLarge({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 57,
      letterSpacing: -0.25,
      height: 64,
);

TextStyle displayMedium({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 45,
      letterSpacing: 0,
      height: 52,
);

TextStyle displaySmall({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 45,
      letterSpacing: 0,
      height: 52,
);

TextStyle headlineLarge({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 32,
      letterSpacing: 0,
      height: 40,
);

TextStyle headlineMedium({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 32,
      letterSpacing: 0,
      height: 40,
);

TextStyle headlineSmall({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 24,
      letterSpacing: 0,
      height: 32,
);

TextStyle titleLarge({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 22,
      letterSpacing: 0,
      height: 28,
);

TextStyle titleMedium({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 16,
      letterSpacing: 0.15,
      height: 28,
);

TextStyle titleSmall({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 14,
      letterSpacing: 0.1,
      height: 20,
);

TextStyle large({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 14,
      letterSpacing: 0.1,
      height: 20,
);

TextStyle medium({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 12,
      letterSpacing: 0.5,
      height: 16,
);

TextStyle small({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 11,
      letterSpacing: 0.5,
      height: 16,
);

TextStyle bodyLarge({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 16,
      letterSpacing: 0.5,
      height: 24,
);

TextStyle bodyMedium({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 14,
      letterSpacing: 0.25,
      height: 20,
);

TextStyle bodySmall({Color colors = Colors.black, String fontPath = defaultFontsPath}) => TextStyle(
      fontFamily: fontPath,
      color: colors,
      fontSize: 12,
      letterSpacing: 0.4,
      height: 16,
);